使用ajax的局部刷新计算bim

1. 新建jsp，使用 XMLHttpRequest异步对象
    使用异步对象有四个步骤：
    1. 创建
    2. 绑定事件
    3. 初始请求
    4. 发送请求

异步对象的原理：一个页面中有许多个xmlHttp异步对象，这些对象自己发送请求自己接收数据，
             自己更新页面，不会影响整个网页的页面。

2.创建服务器的servlet，读取并处理数据，把数据输出异步对象。

